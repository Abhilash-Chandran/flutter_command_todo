name: flutter_command_todo
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_command: ^0.9.3
  get_it: ^5.0.6
  uuid: ^2.2.2
  objectbox: ^0.11.0
  objectbox_flutter_libs: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  objectbox_generator: any

flutter:
  uses-material-design: true
